# Runtime dependencies for the Asterwise MCP server (installed by the Dockerfile).
#
# Pinned exactly. fastmcp 3.4.7 / mcp 1.29.1 is what production runs and what
# the test suite passes against in a clean python:3.12-slim container.
# An unpinned build on 2026-09-03 resolved fastmcp 4.0.2 / mcp 2.1.1, where
# mcp.shared.exceptions.McpError was renamed and the server failed at import.
#
# Bump deliberately: change the pin, rebuild the image, run the tests, deploy.
# Test tooling lives in requirements-dev.txt.
fastmcp==3.4.7
mcp[cli]==1.29.1
uvicorn==0.52.4
httpx==0.28.1
pydantic==2.13.5
python-dotenv==1.2.3
PyJWT==2.13.0
cryptography==50.0.1
